/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from '@solana/web3.js';
import { KASBAH_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const HELLO_DISCRIMINATOR = new Uint8Array([
  149, 118, 59, 220, 196, 127, 161, 179,
]);

export function getHelloDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HELLO_DISCRIMINATOR);
}

export type HelloInstruction<
  TProgram extends string = typeof KASBAH_PROGRAM_ADDRESS,
  TAccountSigner extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            IAccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type HelloInstructionData = {
  discriminator: ReadonlyUint8Array;
  id: number;
};

export type HelloInstructionDataArgs = { id: number };

export function getHelloInstructionDataEncoder(): Encoder<HelloInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['id', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HELLO_DISCRIMINATOR })
  );
}

export function getHelloInstructionDataDecoder(): Decoder<HelloInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['id', getU8Decoder()],
  ]);
}

export function getHelloInstructionDataCodec(): Codec<
  HelloInstructionDataArgs,
  HelloInstructionData
> {
  return combineCodec(
    getHelloInstructionDataEncoder(),
    getHelloInstructionDataDecoder()
  );
}

export type HelloInput<
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  id: HelloInstructionDataArgs['id'];
};

export function getHelloInstruction<
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KASBAH_PROGRAM_ADDRESS,
>(
  input: HelloInput<TAccountSigner, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): HelloInstruction<TProgramAddress, TAccountSigner, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? KASBAH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getHelloInstructionDataEncoder().encode(
      args as HelloInstructionDataArgs
    ),
  } as HelloInstruction<TProgramAddress, TAccountSigner, TAccountSystemProgram>;

  return instruction;
}

export type ParsedHelloInstruction<
  TProgram extends string = typeof KASBAH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    systemProgram: TAccountMetas[1];
  };
  data: HelloInstructionData;
};

export function parseHelloInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedHelloInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getHelloInstructionDataDecoder().decode(instruction.data),
  };
}
